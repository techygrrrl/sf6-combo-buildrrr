/* import { createAppSlice } from './app-slice'

type GlobalConfigState = {}

const initialState: GlobalConfigState = {}

export const globalConfigSlice = createAppSlice({
  name: 'global-config',
  initialState,
  // FYI: async reducers: https://github.com/reduxjs/redux-templates/blob/master/packages/cra-template-redux-typescript/template/src/features/counter/counterSlice.ts#L44
  reducers: (_create) => ({}),
  selectors: {},
})

// Selectors
// todo:

// Actions
// todo:
 */
